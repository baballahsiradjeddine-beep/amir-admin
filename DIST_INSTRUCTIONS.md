# تعليمات هامة لتشغيل التطبيق

مبروك! تم بناء التطبيق بنجاح. إليك ما تحتاج معرفته:

## 📂 موقع التطبيق

ستجد ملف التثبيت في المجلد:
`dist/Amir Nouadi Management Setup 1.0.0.exe`

## 🛠️ متطلبات التشغيل

التطبيق مصمم ليعمل بشكل مستقل، ولكن نظراً لأنه يعتمد على قاعدة بيانات محلية وخادم داخلي، يفضل أن يكون **Node.js** مثبتاً على الجهاز لضمان أفضل توافق، رغم أننا قمنا بدمج الملفات اللازمة.

## 🚀 التشغيل لأول مرة

1. قم بتثبيت التطبيق وتشغيله.
2. ستظهر شاشة التطبيق فارغة للحظات (1-3 ثواني) بينما يعمل الخادم الداخلي، ثم سيظهر التطبيق.
3. قاعدة البيانات سيتم إنشاؤها تلقائياً في بيانات المستخدم.

## ⚠️ حل المشاكل الشائعة

### الشاشة تبقى بيضاء
تأكد من السماح للتطبيق في الجدار الناري (Firewall) لأنه يستخدم منفذ محلي (3000) للاتصال بالخادم الداخلي.

### خطأ في قاعدة البيانات
إذا ظهر خطأ يتعلق بـ `better-sqlite3`، يرجى التأكد من تشغيل التطبيق كمسؤول (Administrator) لأول مرة.

## 💾 النسخ الاحتياطي
التطبيق يقوم بحفظ البيانات في مجلد المستخدم (`AppData`). استخدم ميزة النسخ الاحتياطي داخل التطبيق لحفظ بياناتك في مكان آمن.

بالتوفيق!
