# دليل سريع لتطبيق Electron

## ✅ ما تم إنجازه

1. **إعداد Electron**: تم تثبيت وإعداد Electron بالكامل
2. **الأيقونة**: التطبيق يستخدم `logo.png` كأيقونة
3. **نظام تسجيل الدخول**: يعمل كما هو (نفس صفحة Login)
4. **وضع Offline**: التطبيق يعمل بالكامل بدون إنترنت

## 🚀 كيفية التشغيل

### للتطوير والاختبار:
```bash
npm run electron:dev
```
هذا الأمر سيفتح:
1. Next.js على المنفذ 3000
2. نافذة Electron تعرض التطبيق

### لبناء التطبيق النهائي:
```bash
# 1. أولاً: بناء Next.js
npm run build

# 2. ثانياً: بناء تطبيق Electron
npm run dist
```

سيتم إنشاء ملف التثبيت في مجلد `dist/`

## 📦 الملفات المهمة

- `electron/main.js` - التحكم في نافذة التطبيق
- `electron/preload.js` - الأمان
- `package.json` - إعدادات البناء
- `next.config.js` - إعدادات Next.js للتصدير الثابت

## 🎯 ملاحظات مهمة

1. **قاعدة البيانات**: تُحفظ محلياً في مجلد المستخدم
2. **النسخ الاحتياطي**: يعمل بشكل طبيعي
3. **الصور**: تُحفظ في مجلد `public/uploads`
4. **تسجيل الدخول**: مطلوب عند كل فتح للتطبيق

## 🔧 التخصيص

لتغيير إعدادات التطبيق، عدّل في `package.json`:
- `productName`: اسم التطبيق
- `appId`: معرف التطبيق الفريد
- `build.win.icon`: أيقونة Windows
- `build.mac.icon`: أيقونة macOS

## 📱 أنظمة التشغيل المدعومة

- ✅ Windows (NSIS Installer + Portable)
- ✅ macOS (DMG + ZIP)
- ✅ Linux (AppImage + DEB)

## ⚡ نصائح

- استخدم `npm run electron:dev` أثناء التطوير
- استخدم `npm run dist` لبناء النسخة النهائية
- الملف التنفيذي سيكون في `dist/`
- حجم التطبيق النهائي: ~150-200 MB

---

**جاهز للاستخدام!** 🎉
