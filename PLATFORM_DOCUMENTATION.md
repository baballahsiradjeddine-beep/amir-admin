# توثيق منصة إدارة رأس المال والعملات

## 📊 نظرة عامة على النظام

المنصة مصممة لإدارة رأس مال متعدد العملات مع تتبع الشركات والمزودين والمعاملات. النظام يدير:
- **الصندوق الأساسي (Local Capital)**: رأس المال الأساسي بالدينار الجزائري
- **رأس المال الأجنبي (Foreign Capital)**: الأموال بالعملات الصعبة (USD, EUR, GBP, RMB)

---

## 🏗️ بنية قاعدة البيانات

### 1. جدول `fund_capital` - الصندوق الأساسي
```sql
- id: UUID (المعرّف الفريد)
- user_id: UUID (معرّف المستخدم)
- amount: BIGINT (المبلغ الأساسي بـ DZD)
- password_hash: TEXT (كلمة السر المشفرة لتعديل الصندوق)
- created_at: TIMESTAMP (تاريخ الإنشاء)
- updated_at: TIMESTAMP (تاريخ آخر تحديث)
```

### 2. جدول `companies` - الشركات
```sql
- id: UUID (المعرّف الفريد)
- user_id: UUID (معرّف المستخدم)
- name: TEXT (اسم الشركة)
- owner: TEXT (اسم المالك)
- initial_capital: BIGINT (رأس المال الأولي)
- working_capital: BIGINT (رأس المال العامل)
- share_percentage: INTEGER (نسبة الملكية)
- is_active: BOOLEAN (حالة النشاط)
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

### 3. جدول `fournisseurs` - المزودين
```sql
- id: UUID (المعرّف الفريد)
- user_id: UUID (معرّف المستخدم)
- name: TEXT (اسم المزود)
- currency: TEXT (العملة: USD, RMB, EUR, GBP)
- balance: BIGINT (الرصيد الحالي للمزود)
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

### 4. جدول `transactions` - المعاملات العادية
```sql
- id: UUID (المعرّف الفريد)
- user_id: UUID (معرّف المستخدم)
- type: TEXT (نوع المعاملة: 'company' أو 'fournisseur')
- amount: BIGINT (المبلغ)
- rate: DECIMAL (سعر الصرف المستخدم)
- description: TEXT (وصف المعاملة)
- company_id: UUID (معرّف الشركة إن وجدت)
- fournisseur_id: UUID (معرّف المزود إن وجد)
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

### 5. جدول `currency_companies` - شركات العملات
```sql
- id: UUID (المعرّف الفريد)
- user_id: UUID (معرّف المستخدم)
- name: TEXT (اسم الشركة)
- base_currency: TEXT (العملة الأساسية: USD, EUR, GBP, RMB)
- target_currency: TEXT (العملة المستهدفة: DZD)
- exchange_rate: DECIMAL (سعر الصرف)
- commission_percentage: DECIMAL (نسبة العمولة)
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

### 6. جدول `currency_transactions` - معاملات العملات
```sql
- id: UUID (المعرّف الفريد)
- user_id: UUID (معرّف المستخدم)
- currency_company_id: UUID (معرّف شركة العملة)
- from_amount: DECIMAL (المبلغ الأصلي بالعملة الصعبة)
- to_amount: DECIMAL (المبلغ المحول بـ DZD)
- exchange_rate_used: DECIMAL (سعر الصرف المستخدم)
- commission_amount: DECIMAL (مبلغ العمولة)
- description: TEXT (وصف المعاملة)
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

---

## 💰 دورة حياة المعاملات والتأثير على الأرصدة

### سيناريو 1: إضافة معاملة لشركة (مدخول)
#### الخطوات:
1. المستخدم يضيف معاملة بمبلغ موجب للشركة (مثلاً: +1,000,000 دينار)
2. النظام يقوم بـ:
   - **تخفيض الصندوق الأساسي**: `fundCapital.localCapital -= 1,000,000`
   - **زيادة رأس مال الشركة**: `company.workingCapital += 1,000,000`
   - **حفظ سجل المعاملة** في جدول `transactions`

#### التأثير على البطاقات:
- ✅ بطاقة "الصندوق الأساسي" تنخفض بـ 1,000,000
- ✅ بطاقة "رأس مال الشركات" تزداد بـ 1,000,000
- ✅ إجمالي رأس المال الجديد = الصندوق الأساسي + أرباح الشركات

---

### سيناريو 2: إضافة معاملة لشركة (مخروج/عودة)
#### الخطوات:
1. المستخدم يضيف معاملة بمبلغ سالب للشركة (مثلاً: -500,000 دينار)
2. النظام يقوم بـ:
   - **زيادة الصندوق الأساسي**: `fundCapital.localCapital += 500,000`
   - **تقليل رأس مال الشركة**: `company.workingCapital -= 500,000`
   - **حفظ سجل المعاملة** في جدول `transactions`

#### التأثير على البطاقات:
- ✅ بطاقة "الصندوق الأساسي" تزداد بـ 500,000
- ✅ بطاقة "رأس مال الشركات" تنخفض بـ 500,000

---

### سيناريو 3: إضافة معاملة عملة صعبة (USD → DZD)
#### الخطوات:
1. المستخدم يضيف معاملة عملة: 100,000 USD
   - سعر الصرف: 130 DZD = 1 USD
   - مكتب الصرف المستقبل: "شركة الخليج"
   - مزود يستقبل: 100,000 USD

2. النظام يقوم بـ:
   - **حساب القيمة بالدينار**: `100,000 USD × 130 = 13,000,000 DZD`
   - **زيادة رصيد مكتب الصرف** (مدخول): `+13,000,000 DZD`
   - **زيادة رصيد المزود**: `+100,000 USD`
   - **حفظ سجل المعاملة** في جدول `currency_transactions`

#### التأثير على البطاقات:
- ✅ بطاقة "صندوق العملة الصعبة" تزداد بـ 100,000 USD
- ✅ بطاقة أرصدة مكتب الصرف تزداد بـ 13,000,000 DZD
- ✅ بطاقة أرصدة المزود تزداد بـ 100,000 USD

---

### سيناريو 4: إضافة معاملة لمزود (مدخول)
#### الخطوات:
1. المستخدم يضيف معاملة لمزود بمبلغ موجب (مثلاً: +50,000 USD)
2. النظام يقوم بـ:
   - **زيادة رصيد المزود**: `fournisseur.balance += 50,000`
   - **حفظ سجل المعاملة** في جدول `transactions`

#### التأثير على البطاقات:
- ✅ بطاقة "صندوق العملة الصعبة" تزداد بـ 50,000 USD
- ✅ بطاقة أرصدة المزود تزداد بـ 50,000 USD

---

### سيناريو 5: إضافة معاملة لمزود (مخروج/دفع)
#### الخطوات:
1. المستخدم يضيف معاملة لمزود بمبلغ سالب (مثلاً: -30,000 USD)
2. النظام يقوم بـ:
   - **تقليل رصيد المزود**: `fournisseur.balance -= 30,000`
   - **حفظ سجل المعاملة** في جدول `transactions`

#### التأثير على البطاقات:
- ✅ بطاقة "صندوق العملة الصعبة" تنخفض بـ 30,000 USD
- ✅ بطاقة أرصدة المزود تنخفض بـ 30,000 USD

---

## 📈 حساب البطاقات الرئيسية

### 1. الصندوق الأساسي (Main Fund Card)
```
القيمة = fundCapital.localCapital
```
- يتم تحديثه عند إضافة/حذف معاملة شركة
- يعرض رأس المال الحالي بالدينار الجزائري

### 2. رأس مال الش��كات (Companies Capital Card)
```
القيمة = مجموع workingCapital لجميع الشركات النشطة
```
- يزداد عند إضافة معاملة موجبة لشركة
- ينخفض عند إضافة معاملة سالبة (سحب من الشركة)

### 3. صندوق العملة الصعبة (Foreign Capital Card)
```
القيمة = مجموع أرصدة المزودين (supplier.balance) بالعملات الصعبة فقط
```
- يعرض بـ USD (الدولار الأمريكي)
- يتم حسابه من جدول `fournisseurs` و`balance` field
- لا يشمل معاملات العملات (currency_transactions)

### 4. إجمالي الأرباح (Total Profits)
```
القيمة = (الصندوق الأساسي + رأس مال الشركات + صندوق العملة) - رأس المال الأولي
```
- يعرض الأرباح الكلية للنظام

### 5. رأس المال الجديد (New Capital)
```
القيمة = الصندوق الأساسي + الأرباح
```

---

## 🔄 تدفق المعاملات - Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                       إضافة معاملة جديدة                        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                    ┌────▼────┐
                    │ نوع المعاملة│
                    └────┬────┘
                         │
            ┌────────────┼────────────┐
            │            │            │
      ┌─────▼─────┐ ┌───▼───┐ ┌─────▼─────┐
      │   شركة    │ │ مزود   │ │   عملة    │
      │  (Company)│ │(Fourn.)│ │(Currency) │
      └─────┬─────┘ └───┬───┘ └─────┬─────┘
            │           │           │
      ┌─────▼─────┐ ┌───▼───┐ ┌─────▼──────────┐
      │ المبلغ موجب?│ │موجب؟ │ │ من ← إلى        │
      └─────┬─────┘ └───┬───┘ │ USD ← DZD    │
            │           │     └─────┬──────────┘
      ┌─────▼──────┬─────▼──┐      │
      │مخروج       │ مدخول  │      │
      │(Withdrawal)│(Deposit)│     │
      └─────┬──────┴─────┬──┘     │
            │            │        │
    ┌───────▼────┐ ┌───────▼────┐ │
    │ ↓ رأس مال │ │ ↑ رأس مال  │ │
    │ ↑ صندوق   │ │ ↓ صندوق    │ │
    └───────┬────┘ └───────┬────┘ │
            │              │      │
            └──────────────┼──────┘
                          │
                    ┌─────▼──────┐
                    │ حفظ السجل  │
                    │ في قاعدة   │
                    │ البيانات   │
                    └─────┬──────┘
                          │
                    ┌─────▼──────────┐
                    │تحديث البطاقات  │
                    │والحسابات      │
                    └────────────────┘
```

---

## ❌ الأخطاء الشائعة والحلول

### ❌ الخطأ 1: NaN في عرض الأرصدة
**السبب**: عدم تعيين الصندوق الأساسي بعد
**الحل**: عرض `0` بدلاً من `NaN` باستخدام `isNaN()` check
```javascript
{formatCurrency(isNaN(fundCapital.localCapital) ? 0 : fundCapital.localCapital)}
```

### ❌ الخطأ 2: الشركة المستقبلة ترى مخروج بدلاً من مدخول
**السبب**: الكود كان يعرض المبلغ بالدينار كمخروج
**الحل**: توضيح أن مكتب الصرف تستقبل المبلغ كـ **مدخول** وليس مخروج
```javascript
`→ سيصل لـ ${company.name}: ${amount} DZD (مدخول)`
```

### ❌ الخطأ 3: معاملات تبقى بعد الحذف
**السبب**: البيانات تُحسب من جدول مخزّن وليس من المعاملات الفعلية
**الحل**: حساب البيانات من المعاملات الفعلية فقط:
```javascript
const total = transactions.reduce((sum, t) => sum + t.amount, 0);
```

### ❌ الخطأ 4: دالة `setFundCapital` تعاود استدعاء نفسها
**السبب**: استخدام نفس الاسم للدالة والمتغير
**الحل**: استخدام `setFundCapital()` للدالة و `fundCapital` للبيانات فقط
```javascript
const saveFundCapital = async (capital) => {
  await setFundCapital(user.id, capital, password); // دالة Supabase
  setFundCapitalState(capital); // تحديث الحالة
};
```

---

## 🛠️ خطوات إضافة معاملة جديدة (من البداية)

1. **المستخدم ينقر على "إضافة معاملة"**
2. **يختار نوع الكيان**: شركة / مزود / عملة صعبة
3. **يدخل البيانات المطلوبة**:
   - اسم الشركة/المزود
   - المبلغ
   - الوصف
4. **النظام يتحقق من البيانات**:
   - التحقق من أن المبلغ > 0
   - التحقق من أن الكيان موجود
   - التحقق من صحة العملة
5. **النظام يسجل المعاملة**:
   - إنشاء سجل في الجدول المناسب
   - تحديث أرصدة الكيانات
6. **تحديث البطاقات**:
   - إعادة حساب جميع الإجماليات
   - تحديث الواجهة بالقيم الجديدة
7. **عرض رسالة النجاح**

---

## 📋 الحقول المتاحة في كل كيان

### Company
- `id`, `name`, `owner`, `description`, `initialCapital`, `workingCapital`, `sharePercentage`, `isActive`, `createdAt`

### Fournisseur
- `id`, `name`, `currency`, `balance`, `createdAt`

### Transaction
- `id`, `type` (company/fournisseur), `amount`, `rate`, `description`, `companyId`, `fournisseurId`, `createdAt`

### Currency Company
- `id`, `name`, `baseCurrency`, `targetCurrency`, `exchangeRate`, `commissionPercentage`, `createdAt`

### Currency Transaction
- `id`, `currencyCompanyId`, `fromAmount`, `toAmount`, `exchangeRateUsed`, `commissionAmount`, `description`, `createdAt`

---

## 🔐 الأمان والتحقق

- جميع المعاملات مرتبطة بـ `user_id`
- تعديل الصندوق الأساسي يتطلب كلمة سر
- RLS Policies تضمن أن المستخدم يرى فقط بياناته
- جميع المبالغ تُحفظ كـ BIGINT لدقة عالية

---

## 📊 الحسابات الأساسية

```javascript
// إجمالي الصندوق
totalFund = fundCapital.localCapital

// إجمالي رأس مال الشركات
companiesTotal = companies.reduce((sum, c) => sum + c.workingCapital, 0)

// صندوق العملات الصعبة (USD)
foreignCapitalUSD = fournisseurs.reduce((sum, f) => sum + f.balance, 0)

// الأرباح
totalProfits = totalFund + companiesTotal - initialCapital

// رأس المال الجديد
newCapital = totalFund + totalProfits
```

---

**آخر تحديث**: يناير 2026
