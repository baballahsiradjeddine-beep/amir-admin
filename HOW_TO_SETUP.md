كيفية إعداد التطبيق مع Supabase - دليل شامل

## ما الذي تحتاجه؟

1. حساب Supabase (مجاني)
2. متصفح الويب
3. محرر نصوص (VS Code أو أي برنامج آخر)

---

## الخطوات الكاملة

### 1️⃣ إنشاء مشروع Supabase

1. اذهب إلى: https://app.supabase.com
2. اضغط "New Project"
3. أدخل اس�� المشروع: `the-founders-dz`
4. اختر كلمة مرور قوية
5. اختر المنطقة الأقرب لك (أوروبا أو آسيا)
6. اضغط "Create new project" وانتظر 2-3 دقائق

### 2️⃣ الحصول على بيانات الاتصال

1. في لوحة Supabase، اذهب إلى **Settings** (أسفل يسار الشاشة)
2. اضغط على **API**
3. ستجد:
   ```
   Project URL: https://xxxxx.supabase.co
   Anon Key: eyJxxx...
   ```
4. **انسخ كلا الاثنين** (سنحتاجهما الآن)

### 3️⃣ إنشاء ملف .env.local

1. افتح مجلد المشروع في VS Code
2. أنشئ ملف جديد اسمه `.env.local` (بدون امتداد)
3. الصق الكود التالي:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
   ```
4. استبدل:
   - `your-project` برمز مشروعك (الجزء الأول من الـ URL)
   - `your-anon-key-here` بـ Anon Key كاملاً
5. احفظ الملف (Ctrl+S)

**مثال:**
```
NEXT_PUBLIC_SUPABASE_URL=https://abcxyz123.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 4️⃣ تشغيل SQL Scripts

#### الخطوة أ: إنشاء الجداول

1. في لوحة Supabase، اذهب إلى **SQL Editor** (من الشريط الجانبي)
2. اضغط "New Query"
3. افتح ملف `/scripts/init-database.sql` من المشروع
4. انسخ **كل** محتوى الملف
5. الصقه في نافذة SQL Editor في Supabase
6. اضغط الزر "Run" (أسفل اليسار)
7. انتظر حتى ترى "Success" (قد تستغرق بعض الثواني)

#### الخطوة ب: إنشاء المستخدم التجريبي

1. اضغط "New Query" مرة أخرى
2. افتح ملف `/scripts/create-demo-user.sql`
3. انسخ **كل** محتوى الملف
4. الصقه في نافذة SQL Editor الجديدة
5. اضغط "Run"
6. ستظهر نتيجة بـ UUID المستخدم

**✅ إذا رأيت "Success"، فأنت على الطريق الصحيح!**

### 5️⃣ التحقق من البيانات

1. في Supabase، اذهب إلى **Table Editor**
2. تأكد من وجود 4 جداول:
   - `companies`
   - `fournisseurs`
   - `transactions`
   - `fund_capital`
3. اذهب إلى **Authentication** (من الشريط الجانبي)
4. تأكد من وجود مستخدم واحد: `thefoundersdz@gmail.com`

### 6️⃣ تشغيل التطبيق

في Terminal:

```bash
npm install
npm run dev
```

ستظهر رسالة:
```
> Local:        http://localhost:3000
```

1. افتح http://localhost:3000 في المتصفح
2. يجب أن ترى صفحة تسجيل الدخول

### 7️⃣ تسجيل الدخول

استخدم هذه البيانات:
- **البريد الإلكتروني:** thefoundersdz@gmail.com
- **كلمة المرور:** amirnouadi26

**✅ يجب أن تصل إلى لوحة التحكم الآن!**

---

## 🎉 تم! أنت جاهز

الآن يمكنك:
- إضافة شركات جديدة
- تسجيل معاملات
- عرض التقارير
- جميع البيانات تُحفظ في Supabase

---

## ❌ إذا واجهت مشاكل

### المشكلة: "Missing Supabase environment variables"

**الحل:**
1. تأكد من وجود ملف `.env.local`
2. تأكد من أن البيانات صحيحة (بدون مسافات)
3. أعد تشغيل الخادم: `npm run dev`

### المشكلة: "Invalid UUID"

**الحل:**
1. تأكد من أن SQL script `create-demo-user.sql` تم تنفيذه بنجاح
2. في Supabase > Table Editor > auth > users، تأكد من وجود المستخدم
3. إذا لم يكن موجوداً، شغّل السكريبت مرة أخرى

### المشكلة: "Connection Timeout"

**الحل:**
1. تأكد من الاتصال بالإنترنت
2. تأكد من أن بيانات Supabase صحيحة
3. ابدأ مشروع Supabase جديد وكرر العملية

---

## 📞 الدعم

- ليس لديك حساب Supabase؟ أنشئ واحداً على: https://supabase.com
- هل تحتاج إلى مساعدة؟ اقرأ: TROUBLESHOOTING.md

---

**الآن أنت جاهز لاستخدام التطبيق مع قاعدة بيانات حقيقية! 🚀**
